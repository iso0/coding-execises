# Enter your code here. Read input from STDIN. Print output to STDOUT
N = int(input())
M = input().split(" ")
lisst = []
for i in range(N):
    lisst.append(int(M[i]))
tupple = tuple(lisst)
print(hash(tupple))
